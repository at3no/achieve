<div class="container">
  <h2>ユーザー一覧</h2>
  <% @users.each do |user| %>
      <div class="container-child col-md-2 col-sm-4 col-xs-6">
        <p><%= profile_img(user) %></p>
        <p><%= user.email %></p>
        <p><%= render 'follow_form', user: user unless user == current_user %></p>
        <p><%= link_to "詳細", user_path(user) %></p>
        <p><%= link_to 'メッセージ', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: 'post'%></p>
      </div>
  <% end %>
</div>
