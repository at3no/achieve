<div class="container">
<div class="wrapper  col-md-8 col-md-offset-4 col-sm-10">
<h2>お知らせ一覧</h2>
<ul>
  <% @notifications.each do |notification| %>
  <hr>
  <li style="list-style-type: none; text-align: left;">
    <p style="margin-bottom: 10px; text-align: right;"><span style="padding: 5px; background: #cc6600; color: #fff; border-radius: 6px;"><%= notification.read ? '既読' : '未読' %></span></p>
    <p>
      <%= notification.comment.user.try(:name) %>さんが
      あなたのブログ（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
    </p>
    <p><%= notification.created_at %></p>
  </li>
  <% end %>
</ul>
</div>
</div>
