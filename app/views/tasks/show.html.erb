<div class="container">
<div class="wrapper  col-md-8 col-md-offset-4 col-sm-10" style="text-align: left;">
<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @task.user.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @task.content %>
</p>

<p>
  <strong>Deadline:</strong>
  <%= @task.deadline.strftime("%Y年%m月%d日 %H時%M分") %>
</p>

<p>
  <strong>Charge:</strong>
  <%= @task.charge.name %>
</p>

<p>
  <strong>Status:</strong>
  <%= @task.status == 1? "対応中" : "未着手" %>
</p>


<%= link_to 'Edit', edit_user_task_path(@task.user, @task) %> |
<%= link_to 'Back', user_tasks_path(@task.user) %>
</div>
</div>
